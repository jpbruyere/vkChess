file(GLOB BASE_SRC "*.cpp")
file(GLOB BASE_HEADERS "*.hpp" "vke.h")

ADD_LIBRARY (base STATIC ${BASE_SRC} ${BASE_HEADERS})

TARGET_LINK_LIBRARIES (base
	${Vulkan_LIBRARY}
	glfw
	${CMAKE_THREAD_LIBS_INIT}
)
